# Server Setup Script

Модульный интерактивный менеджер для настройки и управления сервером Debian/Ubuntu.

## 📋 Описание

Скрипт предназначен для автоматизации первоначальной настройки сервера и последующего управления системой через удобное интерактивное меню. Поддерживает настройку безопасности, управление пользователями, настройку SSH, управление файрволом и многое другое.

**Версия:** 3.0-fixed-sftp

## 🔧 Требования

- Операционная система: Debian или Ubuntu
- Права доступа: root (скрипт должен запускаться с правами суперпользователя)
- Интернет-соединение: рекомендуется для установки пакетов

## 📦 Установка

1. Скачайте скрипт:
```bash
wget https://your-server/setup.sh
# или скопируйте файл на сервер
```

2. Сделайте скрипт исполняемым:
```bash
chmod +x setup.sh
```

3. Запустите скрипт с правами root:
```bash
sudo ./setup.sh
```

4. При первом запуске скрипт предложит настроить алиас `setup` для быстрого доступа:
   - Скрипт будет скопирован в `/usr/local/bin/setup.sh`
   - В `/root/.bashrc` будет добавлен алиас `setup`
   - После этого можно запускать скрипт просто командой `setup`

## 🚀 Использование

### Первый запуск

При первом запуске рекомендуется выполнить **Первоначальную настройку** (пункт 1 в главном меню), которая включает:

1. Обновление системы
2. Настройку статического IP (опционально)
3. Оптимизацию сетевых параметров
4. Настройку безопасности SSH
5. Создание пользователя
6. Настройку файрвола (UFW)
7. Настройку времени и локалей

### Главное меню

После запуска скрипта доступны следующие опции:

```
=== УПРАВЛЕНИЕ СЕРВЕРОМ ===
1) Первоначальная настройка
2) Управление портами (UFW)
3) Управление пользователями
4) Управление SSH
5) Просмотр логов
6) Управление задачами Cron
7) Системный мониторинг
8) Обновление системы
9) Проверка безопасности
10) Настроить алиас 'setup'
11) Выход
```

## 📚 Основные функции

### 1. Первоначальная настройка

Комплексная настройка нового сервера:
- Обновление системы
- Настройка статического IP (с предупреждением о возможном разрыве SSH)
- Оптимизация сетевых параметров (sysctl)
- Настройка безопасности SSH
- Создание пользователя с sudo правами
- Настройка базовых правил файрвола
- Настройка времени и локалей

### 2. Управление портами (UFW)

Полноценное меню управления файрволом:

- **Просмотр правил** - показывает текущий статус и все правила
- **Добавить порт** - добавление нового правила с выбором протокола (TCP/UDP/оба) и комментарием
- **Удалить правило** - удаление одного или нескольких правил (номера через пробел)
- **Включить/отключить UFW** - управление состоянием файрвола
- **Сброс к настройкам по умолчанию** - сброс всех правил с сохранением SSH доступа

**Пример удаления нескольких правил:**
```
Введите номер(а) правила для удаления: 2 3 5 6
Будут удалены правила: 2, 3, 5, 6
```

### 3. Управление пользователями

- **Создать/настроить пользователя** - создание нового пользователя с:
  - Установкой пароля
  - Добавлением в группу sudo
  - Настройкой NOPASSWD для sudo
  - Опциональным добавлением SSH-ключа
- **Удалить пользователя** - удаление пользователя с домашней директорией
- **Показать список пользователей**
- **Добавить SSH ключ пользователю**

### 4. Управление SSH

- **Настроить безопасность SSH** - комплексная настройка:
  - Включение/отключение входа по паролю
  - Настройка доступа root
  - Управление SFTP
  - Настройка таймаутов и ограничений
- **Управление входом по паролю** - быстрое переключение
- **Управление SFTP** - включение/отключение SFTP
- **Перезапустить SSH**
- **Показать текущий sshd_config**

**Особенности:**
- Автоматическое создание бэкапа перед изменением конфигурации
- Проверка синтаксиса перед применением изменений
- Автоматический откат при ошибках

### 5. Просмотр логов

Поддержка различных источников логов:
- **auth.log / secure** - логи аутентификации
- **syslog / messages** - системные логи
- **server_setup.log** - логи работы скрипта
- **journalctl** - логи systemd (если доступны)

Скрипт автоматически находит логи в разных местах:
- `/var/log/auth.log` (Debian/Ubuntu)
- `/var/log/secure` (некоторые системы)
- `/var/log/syslog` или `/var/log/messages`
- Использует `journalctl` для систем с systemd

### 6. Управление задачами Cron

- Просмотр задач для root
- Добавление новой задачи
- Редактирование crontab

### 7. Системный мониторинг

Быстрый доступ к информации о системе:
- Статус системы (uptime, load)
- Использование диска
- Использование памяти
- Сетевые соединения
- Топ процессов

### 8. Обновление системы

Полное обновление системы:
- `apt update`
- `apt upgrade`
- `apt autoremove`

### 9. Проверка безопасности

Комплексный аудит безопасности:
- Открытые порты
- Неудачные попытки входа
- Пользователи с SSH-ключами
- Проверка sudo NOPASSWD
- Обновления безопасности (apt)

## ⚙️ Настройки

В начале скрипта можно изменить следующие параметры:

```bash
DEFAULT_SSH_PORT=22                    # Порт SSH по умолчанию
AUTOINSTALL_PACKAGES=true              # Автоматическая установка отсутствующих пакетов
LOG_FILE="/var/log/server_setup.log"   # Путь к файлу логов
SCRIPT_PATH="/usr/local/bin/setup.sh"  # Путь для установки скрипта
ALIAS_NAME="setup"                     # Имя алиаса для быстрого запуска
```

## 🔒 Безопасность

### Особенности безопасности скрипта:

1. **Проверка прав root** - скрипт требует запуска от root
2. **Бэкапы конфигураций** - автоматическое создание бэкапов перед изменением
3. **Проверка синтаксиса** - валидация SSH конфигурации перед применением
4. **Откат при ошибках** - автоматический откат при неудачном применении изменений
5. **Валидация входных данных** - проверка IP, портов, SSH-ключей
6. **Безопасное управление UFW** - не сбрасывает правила без подтверждения

### Рекомендации:

- При настройке статического IP выполняйте скрипт локально (риск разрыва SSH)
- Используйте SSH-ключи вместо паролей
- Отключайте вход root по паролю
- Регулярно проверяйте обновления безопасности

## 📝 Логирование

Скрипт ведёт логи всех действий в:
- `/var/log/server_setup.log` (или `/tmp/server_setup.log` если нет доступа к /var/log)
- Системный журнал через `logger` (если доступен)

## 🐛 Устранение неполадок

### Скрипт не запускается

- Убедитесь, что файл исполняемый: `chmod +x setup.sh`
- Запускайте с правами root: `sudo ./setup.sh`
- Проверьте, что система Debian/Ubuntu

### Ошибки при установке пакетов

- Проверьте интернет-соединение
- Выполните `apt update` вручную
- Установите пакеты вручную, если `AUTOINSTALL_PACKAGES=false`

### Проблемы с SSH после настройки

- Скрипт создаёт бэкапы в `/etc/ssh/sshd_config.backup.*`
- Можно откатить изменения вручную
- Проверьте логи: `tail -f /var/log/auth.log`

### UFW блокирует доступ

- Скрипт не сбрасывает правила автоматически
- При сбросе автоматически добавляется правило для SSH порта
- Проверьте правила: `ufw status numbered`

## 📄 Лицензия

Скрипт предоставляется "как есть" без гарантий.

## 🤝 Вклад

Для улучшения скрипта:
1. Проверьте код на ошибки
2. Протестируйте изменения
3. Убедитесь в совместимости с Debian/Ubuntu

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи: `/var/log/server_setup.log`
2. Проверьте системные логи
3. Убедитесь, что все требования выполнены

---

**Важно:** Всегда тестируйте скрипт на тестовом сервере перед использованием на продакшене!

